# Archived CSS

`styles.css` was never enqueued (1.8.x). Kept here so nothing is missing if a
historical template still references a class from it. Do **not** enqueue.

Public CSS is `tokens.css` + `components.css` + `pages.css` + `chrome.css`.
Rebuild those with `python3 scripts/build-frontend-bundles.py`.
