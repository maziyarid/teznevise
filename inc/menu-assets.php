<?php
/**
 * Enqueue menu assets — retired in 1.8.5.
 * Public CSS/JS is tokens + components + pages + chrome (see functions.php).
 * This file is not required by the theme bootstrap; kept as a no-op so a
 * leftover include cannot resurrect the 24-file cascade.
 *
 * @package Teznevise
 */
function teznevise_enqueue_menu_assets() {
	return;
}
