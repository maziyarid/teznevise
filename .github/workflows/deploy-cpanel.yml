name: Deploy Teznevise theme

# Deployment is handled by the verified VPS cron job.
# This workflow is intentionally disabled to prevent failed API runs.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deployment-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment handled by VPS cron
        run: |
          echo "Deployment is handled by the VPS cron job."
          echo "The server pulls main and runs cPanel deployment every five minutes."
