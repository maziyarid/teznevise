# تزنویسه — React app

TanStack Start companion for teznevise.ir: wallet (Tezcoin), dashboard, tickets vault, tools, and SEO.

```sh
npm install
npm run dev
```

WordPress theme files remain at the repository root. This folder is the React implementation.
